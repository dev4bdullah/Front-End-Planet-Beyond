export default function About() {
  return (
    <div className="card">
      <h3>About</h3>
      <p className="small muted">
        A static page. Its only job here is to prove that a second route renders inside the same
        layout without the sidebar remounting.
      </p>
    </div>
  );
}
